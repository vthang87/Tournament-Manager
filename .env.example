# Local environment — copy to .env and adjust as needed.
# Never commit the real .env file.

# PostgreSQL (local dev server on LAN)
DATABASE_URL=postgresql://tournament:tournament@192.168.0.17:5432/tournament_manager

# Test database (Vitest on host)
TEST_DATABASE_URL=postgresql://tournament:tournament@192.168.0.17:5432/tournament_manager_test

# App URL (used by health checks / public links / QR)
APP_URL=http://localhost:3000

# Signed session cookie secret (min 32 characters). Generate a unique value per environment.
SESSION_SECRET=dev-only-change-me-to-a-long-random-string

# Seed admin credentials (local/dev only)
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=admin123
SEED_ADMIN_DISPLAY_NAME=Local Admin
